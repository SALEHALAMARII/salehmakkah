const menuBtn=document.querySelector('.menu-btn');const drawer=document.querySelector('.mobile-drawer');const overlay=document.querySelector('.menu-overlay');const closeBtn=document.querySelector('.drawer-close');
function setMenu(open){if(!drawer||!overlay||!menuBtn)return;drawer.classList.toggle('open',open);overlay.classList.toggle('open',open);menuBtn.setAttribute('aria-expanded',open?'true':'false');drawer.setAttribute('aria-hidden',open?'false':'true');document.body.classList.toggle('menu-open',open)}
menuBtn?.addEventListener('click',()=>setMenu(!drawer.classList.contains('open')));closeBtn?.addEventListener('click',()=>setMenu(false));overlay?.addEventListener('click',()=>setMenu(false));document.addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});document.querySelectorAll('.drawer-links a,.drawer-cta a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));

// Gallery filters and lightbox
document.querySelectorAll('.gallery-filters button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.gallery-filters button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('.gallery-grid-full .gallery-item').forEach(item=>item.classList.toggle('is-hidden',f!=='all'&&item.dataset.category!==f));}));
const galleryItems=document.querySelectorAll('[data-gallery-src]');if(galleryItems.length){const lb=document.createElement('div');lb.className='lightbox';lb.setAttribute('role','dialog');lb.setAttribute('aria-modal','true');lb.innerHTML='<button class="lightbox-close" aria-label="إغلاق الصورة">×</button><img alt="">';document.body.appendChild(lb);const li=lb.querySelector('img');const close=()=>{lb.classList.remove('open');document.body.classList.remove('menu-open')};galleryItems.forEach(item=>item.addEventListener('click',()=>{li.src=item.dataset.gallerySrc;li.alt=item.dataset.galleryAlt||'';lb.classList.add('open');document.body.classList.add('menu-open')}));lb.addEventListener('click',e=>{if(e.target===lb||e.target.closest('.lightbox-close'))close()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&lb.classList.contains('open'))close()});}
